{
  "name": "codex-helpers",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "lint": "cd packages/codex-auth && bun run lint",
    "typecheck": "cd packages/codex-auth && bun run typecheck",
    "test": "cd packages/codex-auth && bun run test",
    "gate": "cd packages/codex-auth && bun run gate",
    "full-gate": "bun run gate"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
