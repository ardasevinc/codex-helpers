{
  "name": "codex-auth",
  "version": "0.3.2",
  "license": "MIT",
  "type": "module",
  "bin": {
    "codex-auth": "./src/cli.ts"
  },
  "scripts": {
    "lint": "bun run biome check .",
    "typecheck": "bun run tsc --noEmit -p tsconfig.json",
    "test": "bun test",
    "gate": "bun run lint && bun run typecheck && bun run test"
  },
  "dependencies": {
    "@clack/prompts": "^1.0.1",
    "ansis": "^4.2.0",
    "citty": "^0.2.1",
    "is-ai-agent": "^0.1.0"
  }
}
